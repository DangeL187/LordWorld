------------------------------------------------------------
Lord World (LW) by DangeL
------------------------------------------------------------
Player/Player.h <-- Keys
------------------------------------------------------------
Player(float x_, float y_, float w_, float h_)
Default constructor.

void interactionWithMap(auto& map_manager, auto& game)
Handles interaction with obstacles.

update(float time, auto& map_manager, auto& game)
Updates Player.

getSprite()
Return player's sprite.

getX()
Returns player's X coordinate.

getY()
Returns player's Y coordinate.
------------------------------------------------------------