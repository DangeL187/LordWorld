------------------------------------------------------------
Lord World (LW) by DangeL
------------------------------------------------------------
NPC/NPC.h <-- NPCBase
------------------------------------------------------------
NPC(float x_, float y_, float w_, float h_, std::string name_,
auto& NPC_sprites_, auto& other_sprite_counter_,
auto& current_other_sprites_)
Default constructor.

interactionWithMap(auto& map_manager)
Handles interaction with obstacles.

update(auto& time, auto& map_manager, auto& game)
Updates NPC.

startDialog()
Starts dialog with NPC.

getSprite()
Returns NPC's static sprite.

getX()
Returns NPC's X coordinate.

getY()
Returns NPC's Y coordinate.

getName()
Returns NPC's name.
------------------------------------------------------------